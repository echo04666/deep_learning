# llama-cpp-python: native GGUF loader (stays quantized in memory, uses mmap — avoids OOM on Cloud).
# MUST use the prebuilt CPU wheel index — without it, pip/uv compiles llama.cpp from source (~15 min).
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python

# Streamlit
altair>=4.2.0,<5.0.0
streamlit>=1.28.0,<2.0.0

# Shared utilities used by model_utils / app
psutil

# Keep torch + transformers installed for notebook compatibility (not imported by the Streamlit app).
# If Cloud memory is tight, these can be removed — the app only uses llama-cpp-python at runtime.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0,<3.0.0
transformers>=4.40.0,<5.0.0
sentencepiece
datasets
pandas
scikit-learn
accelerate
safetensors
gguf>=0.10.0
